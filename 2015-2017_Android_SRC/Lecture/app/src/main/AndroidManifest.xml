<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.commin.pro.lecture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Application">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.login.Page2Login" />
        <activity android:name=".page.lecture_search.Page2LectureSearch" />
        <activity android:name=".page.lecture.Page2Lecture" />
        <activity android:name=".page.setting.Page2Setting" />
        <activity android:name=".page.sign_up.Page2SignUp"></activity>
    </application>

</manifest>